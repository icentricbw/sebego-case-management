<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Sebego Legal{% endblock %}</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {% block stylesheets %}
        {# Import all styles via AssetMapper #}
        {{ importmap('admin') }}

        <style>
            body {
                background: linear-gradient(135deg, #f5f6fa 0%, #e9ecef 100%);
                min-height: 100vh;
            }

            .auth-card {
                border: none;
                border-radius: 12px;
                overflow: hidden;
            }

            .auth-card .card-header {
                background: linear-gradient(135deg, var(--sebego-primary) 0%, var(--sebego-primary-dark) 100%);
                color: white;
                padding: 2rem;
                border: none;
            }

            .btn-sebego {
                background-color: var(--sebego-primary);
                border-color: var(--sebego-primary);
                color: white;
                padding: 0.75rem 2rem;
                font-weight: 500;
                transition: all 0.3s ease;
            }

            .btn-sebego:hover {
                background-color: var(--sebego-primary-dark);
                border-color: var(--sebego-primary-dark);
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 4px 8px rgba(122, 41, 49, 0.3);
            }

            .form-control:focus {
                border-color: var(--sebego-primary);
                box-shadow: 0 0 0 0.2rem rgba(122, 41, 49, 0.25);
            }

            .logo-container {
                background: white;
                padding: 2rem;
                border-radius: 12px;
                margin-bottom: 2rem;
                box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
            }
        </style>
    {% endblock %}
</head>
<body>
{% block body %}{% endblock %}

{% block javascripts %}
    {{ importmap('admin') }}
{% endblock %}
</body>
</html>
